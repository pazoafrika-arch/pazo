{
  "name": "pazo-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Pazo partner platform - API and web app",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix server install --omit=dev && npm --prefix web install && npm --prefix web run build",
    "start": "npm --prefix server run migrate && npm --prefix server run bootstrap && node server/src/index.js",
    "setup": "npm --prefix server run setup",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "seed": "npm --prefix server run seed"
  }
}